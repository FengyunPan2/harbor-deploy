# Environment file to disable FloatingIP in a Kubernetes cluster by mapping
# FloatingIP-related resource types to OS::Heat::None
resource_registry:
  "Optional::SERVER::Neutron::FloatingIP": "OS::Heat::None"
